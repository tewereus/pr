=> after manager adds mobile the checkManager checks the mobile with the token(unique_id) if not sucessfull goto error page if successfull check the status and redirect based on that(if inactive goto a page to fill their information, if waiting goto page that tells them to wait until active and if active redirect to the main page ) 